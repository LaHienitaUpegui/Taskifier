import "../styles/header.css";

function Header() {
    return (
        <header className="header">
            <p>Taskifier</p>

            <button>Menu</button>
        </header>
    );
}

export default Header;
